import { describe, expect, it } from 'vitest';

// Placeholder to prove the test runner (Vitest + jsdom) is wired up
// correctly, locally and in CI. Replace with real component tests.
describe('frontend test setup', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
